# Copy this to .env and add your actual API keys/credentials

# Gemini API Key (default provider)
GEMINI_API_KEY=your_gemini_api_key_here

# Amazon Bedrock Configuration (alternative provider)
# Setting BEDROCK_MODEL enables Bedrock (no need for BEDROCK_ENABLED)
BEDROCK_MODEL=claude-3-5-sonnet

# AWS Credentials (for Bedrock)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_DEFAULT_REGION=us-east-1
AWS_PROFILE=your_aws_profile_name 